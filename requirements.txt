# graphs
networkx>=3.0

# representation learning
torch>=2.0
torch-geometric>=2.5
transformers>=4.40
tokenizers>=0.19

# utils
numpy
scikit-learn
matplotlib

# tests
pytest>=7

# Disassembly / forensics are NOT listed here on purpose: they are only needed
# on the lab host that owns the VM and the dumps, they are the hardest part of
# the stack to install, and requiring them made a clean checkout unable to run
# anything at all. Install them only when you are actually capturing memory:
#   pip install "smda>=1.13" volatility3
